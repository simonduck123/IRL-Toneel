1. Copy the auth-example.json file
2. Rename the copy to auth.json
3. Update the contents of the auth.json file and save it
4. Drag the auth.json file into the TextAsset field of your NetworkClient instance in the Unity scene

*. Make sure the auth.json file is never committed to GIT!